rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Fichas de comércio: apenas usuários autenticados podem ler e escrever
    match /pautas/{pautaId}/fichas/{filename} {
      allow read, write: if request.auth != null;
    }
  }
}
